"""
Copy this file to `azure_config.py` and fill in values for Azure blob storage
account name, and SAS token
"""

azconfig = {
    "account_name": <ACCOUNT_NAME>,
    "sas_token": <SAS_TOKEN>,
    "account_key": <ACCOUNT_KEY>,
    "connection_string": <CONNECTION_STRING>
}
